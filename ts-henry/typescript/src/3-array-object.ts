// array, object

let names = ['a', 'b', 'c'];
// names.push(5);
// names.push(false);
names.push('d');

let numbers = [1, 2, 3];
// numbers.push('a');
numbers.push(4);

let mixed = [1, 'a', false];
// mixed.push({a: '1', b: 3});
mixed.push(2);
mixed.push('b');
mixed.push(true);

let person = {
    name: 'a',
    age: 1,
    isStudent: false
}

// person.name = 1;
person.name = 'b';

// person.age = 'b';
person.age = 2;

// person = {
//   name: 'c',
//   age: 3
// };

// person = {
//   name: 'c',
//   age: 3,
//   isStudent: true
//   skills: [],
// };

person = {
    name: 'c',
    age: 3,
    isStudent: true
}
